apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: app-logs-pvc
spec:
  storageClassName: ""
  volumeName: app-logs-pv
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi

